% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hobit_mcmc.R
\name{hobit_mcmc}
\alias{hobit_mcmc}
\title{HOBIT Using MCMC}
\usage{
hobit_mcmc(
  x,
  mode = c("strict", "fast"),
  use_Dirichlet = FALSE,
  no_replicate = FALSE,
  eps = 0.001,
  dist = c("NB", "ZINB"),
  n_threads = getOption("mc.cores", 1L),
  parallel_chains = 1L,
  .debug = FALSE,
  ...
)
}
\arguments{
\item{x}{An \linkS4class{SeqCountData} object containing homeolog RNA-seq count
data. The required count representation may depend on \code{mode}.}

\item{mode}{Character string specifying the MCMC fitting mode: \code{"fast"} or
\code{"strict"}. The default is \code{"strict"}.}

\item{use_Dirichlet}{Logical. If \code{TRUE}, use data-derived Dirichlet priors
for homeolog expression ratios.}

\item{no_replicate}{Logical. If \code{TRUE}, use the dispersion-estimation
procedure intended for data without biological replicates.}

\item{eps}{Positive numeric value used as a lower bound for expected
expression values.}

\item{dist}{Character string specifying the distribution used for count
modeling. Available choices are \code{"NB"} and \code{"ZINB"}. Currently, \code{"strict"}
mode supports only \code{"NB"}.}

\item{n_threads}{Positive integer specifying the number of parallel R
workers used across homeolog tuples.}

\item{parallel_chains}{Positive integer specifying the number of Stan chains
run in parallel for each model.}

\item{.debug}{Logical. If \code{TRUE}, return all statistical test results for
detailed analysis or debugging. If \code{FALSE}, return only the main
statistical test results.}

\item{...}{Additional arguments passed to
\code{\link[cmdstanr]{model-method-sample}}, such as \code{chains},
\code{iter_warmup}, \code{iter_sampling}, \code{thin}, and \code{seed}.}
}
\value{
By default, a \code{data.frame} with one row per homeolog containing
\code{gene}, \code{pvalue}, \code{qvalue}, \code{Dmax}, \code{LRmax}, \code{RRmax}, and \verb{theta0__*}
columns. When \code{.debug = TRUE}, the data frame additionally contains:
\itemize{
\item \code{pvalue}: p-value from the MCMC likelihood ratio test (LRT).
\item \code{qvalue}: Benjamini–Hochberg adjusted \code{pvalue}.
\item \code{lrt}: Likelihood ratio statistic.
\item \code{df}: Degrees of freedom used for the LRT.
\item \code{status}: Fitting status for the homeolog.
\item \code{logLik_H0}: Log-likelihood of the reduced model.
\item \code{logLik_H1}: Log-likelihood of the full model.
\item \code{lrt_negative_prob}: Proportion of posterior LRT draws below zero.
\item \verb{D__$__*}: Difference in expression ratios for subgenome \code{$}
between the two groups represented by \code{*}.
\item \verb{LR__$__*}: Log ratio of expression ratios between the
subgenomes and groups represented by \code{$} and \code{*}.
\item \verb{RR__$__*}: Ratio of expression ratios between the
subgenomes and groups represented by \code{$} and \code{*}.
\item \code{Dmax}: Maximum absolute difference in expression ratios (\verb{D__$__*})
observed across all subgenomes and group comparisons.
\item \code{LRmax}: Maximum absolute log ratio in expression ratios (\verb{LR__$__*})
observed across all subgenome-pair and group comparisons.
\item \code{RRmax}: Maximum ratio in expression ratios (\verb{RR__$__*})
observed across all subgenomes and group comparisons.
\item \verb{theta0__$}: Posterior expression ratio estimates shared
across all conditions, where \code{$} denotes the subgenome name.
\item \verb{theta1__$__*}: Posterior expression ratio estimates specific
to each condition,
where \code{$} denotes the subgenome name and \code{*} denotes the group name.
}
All statistics are derived from MCMC samples and may differ from
statistics calculated directly from RNA-seq read counts. An \code{NA} value
indicates that the homeolog is expressed in only one condition, making
ratio comparisons infeasible.
}
\description{
Detects changes in homeolog expression ratios using an MCMC-based
implementation of HOBIT.
}
\details{
The \code{mode} argument selects either the faster approximate workflow or the
strict workflow, which fits the null and alternative models independently.
Detailed methodological background and usage guidance are available at
\url{https://bitdessin.github.io/hespresso/}.
}
\examples{
x <- sim_homeolog_counts(10)
x_output <- hobit_mcmc(x)

# strict mode (default)
x_output <- hobit_mcmc(x, chains = 2, iter_warmup = 100, iter_sampling = 100)

# fast mode
x_output <- hobit_mcmc(x, mode = "fast", chains = 2, iter_warmup = 100, iter_sampling = 100)

# parallel processing
x_output <- hobit_mcmc(x, n_threads = 1, parallel_chains = 8,
                       iter_warmup = 100, iter_sampling = 100)
x_output <- hobit_mcmc(x, n_threads = 8, parallel_chains = 1,
                       iter_warmup = 100, iter_sampling = 100)

}
\references{
Sun J, Sese J, Shimizu KK.
A likelihood ratio test for detecting shifts in homeolog expression ratios in allopolyploids.
New Phytol., 249(1):603-617, 2026.
\doi{10.1111/nph.70648}
}
\seealso{
\code{\link[=hobit]{hobit()}}, \code{\link[=hobit_mle]{hobit_mle()}}
}
