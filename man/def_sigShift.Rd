% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{def_sigShift}
\alias{def_sigShift}
\title{Define Ground-Truth Homeolog-Expression-Ratio Shifts}
\usage{
def_sigShift(
  x,
  base = 0,
  groups = NULL,
  Dmax = NULL,
  ORmax = NULL,
  operator = c("OR", "AND")
)
}
\arguments{
\item{x}{An \code{SeqCountData} object generated by \code{\link[=sim_homeolog_counts]{sim_homeolog_counts()}}.}

\item{base}{Integer subgenome index used for thresholding. If \code{0}, the
maximum value across all subgenomes is used.}

\item{groups}{Optional character vector of length two specifying the
conditions to compare. If \code{NULL}, all condition pairs are considered.}

\item{Dmax}{\code{NULL} or a numeric threshold for the maximum absolute
homeolog-expression-ratio difference.}

\item{ORmax}{\code{NULL} or a numeric threshold for the maximum one-versus-rest
odds ratio of homeolog-expression-ratio changes.}

\item{operator}{Character string specifying how \code{Dmax} and \code{ORmax} criteria
are combined when both are supplied: either \code{"OR"} or \code{"AND"}.}
}
\value{
A named logical vector. If both thresholds are \code{NULL} and \code{base} is
\code{0}, it is exactly \code{x@meta@is_shift}. For a specified base, it contains
the global truth labels restricted to genes whose selected subgenome's
ratio changes between conditions. Otherwise, it indicates genes passing
the supplied threshold or thresholds.
}
\description{
Returns the exact simulated homeolog-expression-ratio shift labels when
\code{Dmax} and \code{ORmax} are both \code{NULL}. If \code{base} is specified, the global truth
labels are restricted to genes whose selected subgenome's ratio changes
between conditions. When either threshold is supplied, the function
calculates ratio differences from the true homeolog expression ratios and
filters genes using the supplied threshold or thresholds.
}
\examples{
x <- sim_homeolog_counts(n_genes = 100)

# Exact simulation truth.
is_shift <- def_sigShift(x)

# Effect-size filtering.
is_large_shift <- def_sigShift(x, Dmax = 0.15, ORmax = 2)

}
\seealso{
\code{\link[=sim_homeolog_counts]{sim_homeolog_counts()}}
}
